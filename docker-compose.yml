services:
   api:
      build:
         context: .
         dockerfile: Dockerfile
         args:
            DATABASE_URL: ${DATABASE_URL}
      container_name: sabledream_api
      restart: unless-stopped
      ports:
         - "5001:5001"
      env_file:
         - .env

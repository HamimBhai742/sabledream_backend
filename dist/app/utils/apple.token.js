"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.verifyAppleToken = void 0;
require("../config/firebase"); // Ensures firebase-admin app is initialized
const firebase_admin_1 = __importDefault(require("firebase-admin"));
const AppError_1 = __importDefault(require("../error/AppError"));
const http_status_1 = __importDefault(require("http-status"));
/**
 * Verifies a Firebase ID Token generated by Apple sign-in.
 *
 * @param idToken - The Firebase ID Token sent from the client.
 * @returns The decoded Firebase ID Token payload.
 */
const verifyAppleToken = (idToken) => __awaiter(void 0, void 0, void 0, function* () {
    var _a;
    try {
        const decodedToken = yield firebase_admin_1.default.auth().verifyIdToken(idToken);
        // Verify that the provider is apple.com
        const signInProvider = (_a = decodedToken.firebase) === null || _a === void 0 ? void 0 : _a.sign_in_provider;
        if (signInProvider !== "apple.com") {
            throw new AppError_1.default(http_status_1.default.UNAUTHORIZED, "The provided token is not from Apple Sign-in via Firebase");
        }
        return decodedToken;
    }
    catch (error) {
        console.error("[APPLE FIREBASE AUTH] Error verifying token:", error);
        throw new AppError_1.default(http_status_1.default.UNAUTHORIZED, `Apple verification failed: ${error.message}`);
    }
});
exports.verifyAppleToken = verifyAppleToken;
